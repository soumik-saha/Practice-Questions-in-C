#include<stdio.h>
#include<string.h>
int main()
{
    char str[50];
    printf("Enter a string : ");
    scanf("%s",str[50]);
    printf("%s",)
    int length=strlen(str);
    // printf("The length of the %s is %d",str[50],length);
    return 0;
}